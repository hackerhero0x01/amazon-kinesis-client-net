<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <Target Name="CustomPreBuildTarget" BeforeTargets="Build">
    <Exec Command="echo Hello World from Pre-Build!"/>
  </Target>

  <Target Name="CustomPostBuildTarget" AfterTargets="Build">
    <Exec Command="echo Hello World from Post-Build!"/>
  </Target>

</Project>
